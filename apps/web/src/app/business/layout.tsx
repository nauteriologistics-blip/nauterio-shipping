import type { Metadata } from "next";

export const metadata: Metadata = { title: "Business Shipping", description: "Discuss repeat international shipping routes, cargo profiles and account arrangements with Nauterio." };
export default function BusinessLayout({ children }: { children: React.ReactNode }) { return children; }
