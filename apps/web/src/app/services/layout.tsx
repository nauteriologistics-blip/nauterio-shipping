import type { Metadata } from "next";

export const metadata: Metadata = { title: "Air and Ocean Freight Services", description: "Compare Nauterio Air Express, Air Economy and consolidated ocean freight services." };
export default function ServicesLayout({ children }: { children: React.ReactNode }) { return children; }
