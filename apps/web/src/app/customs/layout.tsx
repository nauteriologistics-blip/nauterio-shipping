import type { Metadata } from "next";

export const metadata: Metadata = { title: "Customs Preparation", description: "Prepare accurate shipment descriptions and documents and understand common international shipping restrictions." };
export default function CustomsLayout({ children }: { children: React.ReactNode }) { return children; }
