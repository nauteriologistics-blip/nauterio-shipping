import type { Metadata } from "next";

export const metadata: Metadata = { title: "Plan a Shipment", description: "Enter cargo and route details to prepare an international shipment with Nauterio." };
export default function QuoteLayout({ children }: { children: React.ReactNode }) { return children; }
