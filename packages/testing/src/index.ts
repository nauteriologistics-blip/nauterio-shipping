/**
 * Shared test fixtures and factories (ADR 0001 section 3.1). Production
 * data must never be copied into development/test (spec section 32.2) -
 * every fixture here is synthetic.
 */
export * from "./fixtures";
