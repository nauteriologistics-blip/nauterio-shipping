# Operational runbooks

Create runbooks for every critical alarm, dependency, queue, backup/restore procedure, security incident, carrier outage, notification failure, document scan failure, and deployment rollback. Online payment is intentionally disabled in the current operating model; see `no-payment-operating-model.md`.
