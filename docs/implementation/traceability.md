# Requirements traceability

| Requirement | Specification section | Implementation | Tests | Status | Evidence | Dependency |
|---|---|---|---|---|---|---|
| Permission chain: account status → role baseline → org scope → warehouse scope → record relationship → approval limit → separation of duties | Spec §27.3 | `packages/validation/src/permission-evaluator.ts`, `apps/api/src/common/guards/{auth,permission}.guard.ts` | `packages/validation/src/permission-evaluator.test.ts` (11 tests), live repro in `docs/audit/BACKEND_AUDIT_EVIDENCE.md` | Done — every step in the chain now fails closed on missing context; customer baseline allowlist added; SEC-010 item 5's per-action required-scope metadata table (`REQUIRED_SCOPE_FIELDS`) now built | See `docs/audit/BACKEND_AUDIT_VERIFICATION.md` SEC-002/003/009/010, DATA-006 | None |
| Idempotency-Key enforcement, scoped per caller | Spec §26.1 | `apps/api/src/common/interceptors/idempotency.interceptor.ts`, `packages/database/prisma/schema.prisma` (`IdempotencyRecord`) | `apps/api` Jest suite (interceptor exercised via booking/claim service specs) | Done — `(scope, userId, key)` uniqueness, `FAILED` terminal state, TTL sweep job | See `docs/audit/BACKEND_AUDIT_VERIFICATION.md` SEC-005/DATA-001/002/003 | None |
| Tracking, audit, and issued financial history are append-only | CLAUDE.md (unconditional) | `packages/database/prisma/migrations/20260809120500_tracking_integrity_and_indexes` (`tracking_events_append_only` trigger) | Live `psql` repro in `docs/audit/BACKEND_AUDIT_EVIDENCE.md` | Done for `tracking_events`; `audit_events`/`inbox_events`/`payment_events`/`package_movements` were already correct | See `docs/audit/BACKEND_AUDIT_VERIFICATION.md` DATA-008 | None — SEC-011's erasure-vs-append-only FK collision resolved via ADR 0002 (pseudonymisation, `docs/decisions/0002-gdpr-erasure-via-pseudonymisation.md`) and `POST /v1/admin/users/:id/erase`, live-verified against a user with real audit history |
| Money is always integer minor units + ISO currency, never float | CLAUDE.md (unconditional) | `apps/api/src/modules/quotes/quotes.service.ts`; currency-format CHECKs on all 15 currency columns (`20260809160000_currency_format_constraints`); race-safe aggregate triggers (`20260809182000_payment_aggregate_constraints`) | `quotes.service.spec.ts` (7 tests); live repro confirming `3611` not `3610`; live repro confirming `currency='zz9'`/`'eur'` rejected, `'USD'` accepted; live-reproduced two genuine concurrent-write races against real simultaneous Postgres sessions, confirming the `FOR UPDATE`-locked triggers serialize correctly | Done — quotes, format validation everywhere, and refund/payment-allocation aggregate constraints. Cross-table currency-agreement constraints (e.g. `payment.currency = payment_allocation.currency`) still intentionally not added — that needs a trigger, not a same-table CHECK, and remains lower priority than the aggregate-sum invariant this pass closed | See `docs/audit/BACKEND_AUDIT_VERIFICATION.md` DATA-010, DATA-011(b), DATA-018 | None |
| Transactional outbox + idempotent consumers | Spec §29.1-29.2 | `apps/worker/src/outbox-relay.ts`, `apps/worker/src/queue/local-queue-adapter.ts`, `apps/worker/src/jobs/notifications-email.job.ts` (`resolveNotification`) | Live-verified end to end: `BookingsService.confirmBooking` writes a real `outbox_events` row, the relay picks it up, and `notifications-email`'s handler now resolves `shipment.created`'s actual payload shape (looks up the shipment's owner user) instead of expecting the old pre-resolved shape | Done — relay/consumer hardened for multi-replica/at-least-once delivery; `shipment.created` given its own resolution path in the one real consumer that receives it, closing the payload-shape mismatch REL-020's re-verification surfaced | See `docs/audit/BACKEND_AUDIT_VERIFICATION.md` DATA-004/005, REL-005/006/008/020 | None |
| CI lint gate | CLAUDE.md delivery workflow step 9 | `eslint.config.mjs` (root, shared by `apps/api`/`apps/worker`), now including `no-console` | `pnpm -r lint` exits 0 | Done | See `docs/audit/BACKEND_AUDIT_VERIFICATION.md` REL-019, REL-011 | None |
| Structured JSON logging with ambient correlation ID | ADR 0001 §10 | `packages/observability/src/index.ts` (`createLogger`, `NestPinoLoggerAdapter`); `apps/api/src/logger.ts`; `apps/worker/src/logger.ts` | Live-verified: booted API/worker both emit structured JSON; a genuine 500 produced a full `err.stack` field; worker handler failure logged `correlationId`/`queue`/`messageId` from the real message | Done — Nest's own internal logs and every application log call now go through pino; `console.*` eliminated and blocked by lint | See `docs/audit/BACKEND_AUDIT_VERIFICATION.md` REL-011 | None |
| Customer sign-in/sign-up (spec screens 75/76) and portal auth gate | Spec §27.1, Appendix A rows 75-76 | `apps/api/src/modules/auth/auth.module.ts` (`POST /v1/auth/register`, `POST /v1/auth/verify-email`); `packages/database/prisma/migrations/20260809190000_email_verification_tokens`; `apps/web/src/app/{signin,register,verify-email}/page.tsx`; `apps/web/src/app/api/auth/*`, `apps/web/src/app/api/v1/[...path]/route.ts` (BFF session, mirrors `apps/admin`'s SEC-015 pattern); `apps/web/src/app/portal/layout.tsx` (server-side session gate, replacing the previous no-guard-at-all state) | Live-verified end to end in a real browser: register → dev-mode verification link → verify (establishes session) → portal renders with real profile → sign out → session genuinely cleared server-side (confirmed via a post-logout reload bouncing to `/signin`, not just client state) | Done for the dev-mode identity model (no real Cognito pool exists in this environment - see `infra/cdk/lib/compute-stack.ts`'s own comment). Caught and fixed a real concurrency bug during verification: two near-simultaneous verify-email requests for the same token (React StrictMode's double-effect-invoke) could both succeed before the fix - closed with an atomic conditional `updateMany` server-side plus a client-side single-fire guard. Password reset not built - there is no app-managed password in this model to reset. Email verification is stubbed (dev-mode link shown directly, not emailed) - `REQUIRES_BUSINESS_EVIDENCE`: a real SES sending identity | See `docs/audit/FRONTEND_UX_GAPS.md` for the pre-fix state and the scope decisions this was built against | Real Cognito Hosted UI integration and business/organisation registration remain out of scope, per the explicit scope decision recorded when this was built |
| Customer portal dashboard, spec's exact section order | Spec §10.1 | `apps/web/src/app/portal/page.tsx`, `apps/web/src/lib/portal-data.server.ts`; new `GET /v1/documents` (apps/api `documents.module.ts`, `listMine`) since no "my documents" list endpoint existed before | Live-verified with a real signed-in account: every section (active shipments, upcoming pickups/deliveries, unpaid/customs charges, recent documents/invoices, recent claims, route guidance) renders from real API calls with correct empty states, not the previous 100% hardcoded mock data | Done - all 8 of spec 10.1's sections now present and real (previously only 3 of 8 existed, and all were hardcoded). Document upload intentionally left as a disclosed "not available yet" state rather than faking success - real upload needs the quarantine/malware-scan pipeline (spec 28.1), which needs S3/KMS infrastructure not provisioned here | See `docs/audit/FRONTEND_UX_GAPS.md` §3 for the pre-fix state | None |
| Language switching (EN/IT) | Spec: English and Italian from launch (executive product definition) | `next-intl` wired without URL-prefixed routing (cookie-based, `apps/web/src/i18n/{config,request}.ts`); `apps/web/messages/{en,it}.json`; `apps/web/src/components/Header.tsx` and every auth/portal page use `useTranslations`/`getTranslations` | Live-verified bidirectionally in a real browser: header, sign-in, register, verify-email, and the full portal dashboard all render correctly in Italian after switching, and correctly back in English | Done for nav, portal, and the three auth pages. Marketing pages (`/`, `/services`, `/quote`, `/tracking`, etc.) and legal pages (`/terms`, `/privacy`, `/cookies`) remain English-only - legal pages deliberately held back for native/legal review before translating (mistranslating legal text is a real risk), marketing/transactional pages deferred for scope, not held back on principle | See `docs/audit/FRONTEND_UX_GAPS.md` §6 question 4 for the scope decision | Marketing/legal page translation |
