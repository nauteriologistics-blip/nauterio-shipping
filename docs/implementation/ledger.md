# Implementation ledger

Record each approved work package, owner, branch/PR, status, completed evidence, unresolved risks, and next dependency.

| Work package | Owner | Status | Code/PR | Test evidence | Open risks | Next dependency |
|---|---|---|---|---|---|---|
| Backend architecture plan (ADR 0001) | Claude (planning) | Proposed, pending approval | `docs/decisions/0001-backend-architecture-plan.md` | N/A — planning doc, no code | Monorepo restructure is unimplemented; §11 open questions need business answers | Product Owner / technical lead review and sign-off before any monorepo restructure or `apps/api` work begins |
| Backend audit remediation, first pass (permission model, idempotency, async pipeline, money arithmetic, tracking data integrity, cross-cutting error handling, CDK infra, backend lint) | Claude | 40/56 findings fixed and verified, 8 partially fixed, 13 deferred, 1 informational | Superseded by the second pass below; migrations `20260809105340_idempotency_per_principal`, `20260809110905_outbox_reliability`, `20260809120500_tracking_integrity_and_indexes` | `pnpm -r typecheck`/`test`/`lint` clean; live reproductions in `docs/audit/BACKEND_AUDIT_EVIDENCE.md` (first-pass section) | Superseded — see second pass | Second pass below |
| Backend audit remediation, second pass (GDPR erasure/ADR 0002, audit-trail coverage, currency constraints, trust-proxy fix + Redis-backed throttler wiring, structured logging via pino, `desiredCount` 2, RDS log retention + Budgets alarm, `CONCURRENTLY` index residual, quote-expiry sweep) | Claude | 43/56 findings fixed and verified, 5 partially fixed, 5 deferred, 3 informational/scope-not-built | Superseded by the third pass below; touches `apps/api`, `apps/worker`, `packages/{contracts,configuration,observability,database}`, `infra/cdk`; new ADR `docs/decisions/0002-gdpr-erasure-via-pseudonymisation.md`; new runbook `docs/runbooks/database-migrations.md`; migrations `20260809150000_concurrent_indexes`, `20260809151500_quote_expiry_index`, `20260809153000_gdpr_erasure_pseudonymisation`, `20260809160000_currency_format_constraints` | `pnpm -r typecheck`/`test`/`lint` clean (including `no-console` now enforced); live reproductions for every fixed/partially-fixed item in `docs/audit/BACKEND_AUDIT_VERIFICATION.md` | Superseded — see third pass | Third pass below |
| Backend audit remediation, third pass (DATA-015 least-privilege role/GRANTs, REL-020 `shipment.created` handler, REL-016/REL-017 residuals, SEC-010 item 5, SEC-008/REL-004 Redis cross-instance verification + token-hash tracker, REL-014 RDS Proxy ADR 0003, DATA-007/009/017/018/019, SEC-015 admin BFF auth rebuild, SEC-016 dependency upgrades) | Claude | 55/56 findings fixed and verified; the 1 remaining (DATA-004's original "no producer" framing) is genuinely N/A now that REL-020 is FIXED — 0 findings partially fixed or deferred | Uncommitted on `main` as of this entry; touches `apps/api`, `apps/admin` (full auth rebuild), `apps/worker`, `packages/{validation,database}`, `package.json`/`pnpm-workspace.yaml`; new ADR `docs/decisions/0003-rds-proxy-prepared-statement-pinning-resolved.md`; migrations `20260809170000_app_role_least_privilege`, `20260809180000_missing_fk_constraints`, `20260809181000_tracking_event_dedup_key`, `20260809182000_payment_aggregate_constraints`, `20260809183000_shipment_package_aggregates` | `pnpm -r typecheck`/`lint`/`test`/`build` clean; live reproductions for every item in `docs/audit/BACKEND_AUDIT_VERIFICATION.md`, including two genuine concurrency races against real simultaneous Postgres sessions, cross-instance rate-limiting against a real local Redis shared by two real API processes, and a full browser-driven login→dashboard→sign-out click-through for SEC-015 | Full disposition in `docs/audit/BACKEND_AUDIT_VERIFICATION.md`; two real bugs found and fixed mid-pass, not pre-planned: `apps/admin`'s CSP missing `unsafe-eval` in dev (broke React/Turbopack's own debugging tooling), and Vitest 4 refusing to load stale `tsc`-compiled `dist/*.test.js` files that Vitest 2 had silently tolerated | Nothing tracked in `docs/audit/BACKEND_AUDIT_FIX_PLAN.md` remains; the one incremental step outside this pass's scope is wiring `pnpm audit --audit-level=high` into CI, and DATA-015's own disclosed gap (no CDK-wired credential-switching to the new least-privilege role in production) |
| `apps/web` customer auth, portal dashboard rebuild, and i18n (see `docs/audit/FRONTEND_UX_GAPS.md`) | Claude | Sign-in/sign-up/portal/language all rebuilt from non-functional to working and live-verified; scope-limited by explicit decision (dev-mode identity, not real Cognito; individual signup only; email verification stubbed; UI-string translation only, legal pages held back) | Uncommitted on `main` as of this entry; new `apps/api/src/modules/auth/auth.module.ts`; `GET /v1/documents` added to `documents.module.ts`; `apps/web/src/app/{signin,register,verify-email}/page.tsx`, `apps/web/src/app/portal/{layout,page}.tsx` rewritten, `apps/web/src/lib/{session,session.server,auth,portal-data.server}.ts` new; `apps/web/src/i18n/{config,request}.ts` + `apps/web/messages/{en,it}.json` new; `next-intl` added; migration `20260809190000_email_verification_tokens` (also closes a DATA-015 gap: the least-privilege role's `GRANT ON ALL TABLES` only covered tables existing at the time it ran — added `ALTER DEFAULT PRIVILEGES` so this doesn't recur for every future migration) | `pnpm -r typecheck`/`lint`/`test`/`build` clean; full real-browser click-through: register → verify → portal (real empty-state data) → booking wizard through all 3 real steps → sign out → sign back in → language switch both directions, all against the real API and a real local Postgres, not mocked | Three real bugs found and fixed mid-build, not pre-planned: (1) email-verification token consumption had a TOCTOU race — caught via React StrictMode double-firing the verify request twice in the browser, both succeeding; fixed with an atomic conditional `updateMany` server-side plus a client single-fire guard. (2) The booking wizard's `currentStep` sent `"STEP_1"`/`"STEP_2"` literals that never matched the real `BookingStep` enum, so every draft save 500'd the moment it was reached through a real session (previously masked entirely by a hardcoded dev bearer token). (3) The wizard also re-POSTed a new `Booking` row on every step instead of updating the existing draft, leaving 2-3 orphaned rows per session — now uses `PATCH` once a draft exists | Real Stripe payment integration (booking confirmation correctly 400s without it — the API refuses to infer payment success without real provider confirmation, so this is disclosed in the UI rather than faked); real Cognito Hosted UI; business/organisation registration; marketing and legal page translation |
